.global entry
entry:
    addi a0, zero, 12
    
    lb   s0, 4(a0)

    addi zero, zero, 10
    addi zero, zero, 10
    addi zero, zero, 10
    addi zero, zero, 10
    addi zero, zero, 10
    addi zero, zero, 10
    addi zero, zero, 10
    addi zero, zero, 10
    addi zero, zero, 10
    addi zero, zero, 10
    addi zero, zero, 10
    addi zero, zero, 10
    addi zero, zero, 10
    addi zero, zero, 10
    addi zero, zero, 10
    addi zero, zero, 10


