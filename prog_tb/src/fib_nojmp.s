.global entry
entry:
    addi s0, zero, 1
    addi s1, zero, 1

    sub  zero, s0, zero

    addi s2, s1, 0
    add  s1, s0, s1
    addi s0, s2, 0

    sub  zero, s0, zero

    addi s2, s1, 0
    add  s1, s0, s1
    addi s0, s2, 0

    sub  zero, s0, zero

    addi s2, s1, 0
    add  s1, s0, s1
    addi s0, s2, 0

    sub  zero, s0, zero

    addi s2, s1, 0
    add  s1, s0, s1
    addi s0, s2, 0

    sub  zero, s0, zero

    addi s2, s1, 0
    add  s1, s0, s1
    addi s0, s2, 0

    sub  zero, s0, zero

